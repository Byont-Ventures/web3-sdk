import type { NextPage } from 'next';
import Connector from '../components/Connector';

const Home: NextPage = () => {
  return (
    <div>
      <Connector />
    </div>
  );
};

export default Home;
